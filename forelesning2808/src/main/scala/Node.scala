/*object Node extends App{
  // println(recursive_func(0)) // brukes i main:P
  def recursive_func(i: Int): Int ={
    if(i > 10)
      return i
    else
      recursive_func(i + 1)
  }

  def maxi(number1: Int, number2: Int): Int ={
    if (number1 > number2) number1 else number2
  }
}*/

/* hvordan jeg fikset sdk feil:
Fikse No Scala SDK feil:
gå til Prosjekt settings->libraries og legg til scala
jeg valgte å bruke coursier
 */